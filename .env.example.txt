# ============================================================
# Endgame Sniper Bot - Environment Configuration
# Copy this file to .env and fill in your values
# ============================================================

# ==================== WALLET ====================
# Your private key (required)
# WARNING: Keep this secret! Never share or commit to git
PRIVATE_KEY=your_private_key_here

# Polymarket proxy address (for smart contract wallet)
# Leave empty if using EOA wallet directly
POLY_PROXY_ADDRESS=

# ==================== PROXY CONFIGURATION ====================
# Proxy is HIGHLY RECOMMENDED when running on VPS to avoid Cloudflare blocks
# 
# Option 1: Single proxy for both HTTP and HTTPS (recommended)
# Format: protocol://[user:password@]host:port
# Examples:
#   HTTP proxy:     http://user:pass@proxy.example.com:8080
#   HTTPS proxy:    https://user:pass@proxy.example.com:8080
#   SOCKS5 proxy:   socks5://user:pass@proxy.example.com:1080
#   No auth:        http://proxy.example.com:8080
PROXY_URL=

# Option 2: Separate proxies for HTTP and HTTPS
# Only used if PROXY_URL is empty
PROXY_HTTP=
PROXY_HTTPS=

# ==================== TELEGRAM NOTIFICATIONS ====================
# Optional: Get alerts when orders are placed/filled
# Create a bot via @BotFather and get your chat ID via @userinfobot
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# ============================================================
# PROXY RECOMMENDATIONS FOR VPS
# ============================================================
# 
# 1. Residential Proxy (Best for avoiding blocks):
#    - BrightData, Oxylabs, SmartProxy
#    - More expensive but very reliable
#
# 2. Datacenter Proxy (Cheaper):
#    - May still get blocked occasionally
#    - Good for testing
#
# 3. SOCKS5 Proxy:
#    - Works well with most providers
#    - Format: socks5://user:pass@host:port
#
# Example providers:
#    - BrightData: https://brightdata.com
#    - Oxylabs: https://oxylabs.io  
#    - IPRoyal: https://iproyal.com
#    - Webshare: https://webshare.io
#
# TIP: Test your proxy first with:
#    curl -x http://user:pass@host:port https://polymarket.com
# ============================================================
